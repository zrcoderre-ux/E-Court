# LA Court E-Court Suite — mail-merge recipient config (LOCAL, not committed).
#
# Copy this file to:   %USERPROFILE%\ecourt-recipient.txt
# and fill in real values. The MailMerge_AutoMacro reads it to address the
# recommendation email. key=value lines; leave "email" blank to leave the
# Outlook "To" field empty.
#
email=name@example.com
salutation=Judge Smith
signoff=First
